#include <iostream>
using namespace std;

int main(){
int arr[]={5,56,100,250};
int len = sizeof(arr)/sizeof(int);
int max = arr[0];
for(int idx=0; idx<len; idx++){
    if(arr[idx] > max){
        max = arr[idx];
        }

}
 cout<< "The largest number is" "= "<< max <<endl;
return 0;
}


