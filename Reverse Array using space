#include<iostream>
using namespace std;

printArr(int arr[], int n){
for(i=0;i<n;i++){
cout<<arr[i]<<endl;
}
cout <<endl;
}
int main(){
int arr[]={5,6,7,8,9};
int n = sizeof(arr)/sizeof(int);
int CopyArr[n];
for(i= 0; i <n ; i++){
j = n -i-1;
CopyArr[i]=arr[j];
}
for(i=o;i<n;i++){
arr[i]=CopyArr[i];
}
printArr(arr, n);
return 0;
}
